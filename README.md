HeLLo to best logistics site you can use and edit
Design by DR:Gamal Saad.
# داش كورد - دليل رفع الموقع على GoDaddy

## 📋 نظرة عامة

هذا الموقع هو موقع HTML ثابت يحتوي على صفحات متعددة لإدارة اللوجستيات. يمكن رفعه على GoDaddy بعدة طرق.

---

## 🚀 الطريقة الأولى: رفع الملفات عبر cPanel File Manager

### المتطلبات:
- حساب GoDaddy نشط
- اسم نطاق (Domain) مرتبط بحساب GoDaddy
- استضافة (Hosting) نشطة

### الخطوات:

#### 1. تسجيل الدخول إلى cPanel
- اذهب إلى [GoDaddy.com](https://www.godaddy.com)
- سجل الدخول إلى حسابك
- اذهب إلى **My Products** (منتجاتي)
- اضغط على **Manage** (إدارة) بجانب الاستضافة
- اختر **cPanel Admin** أو **File Manager**

#### 2. الوصول إلى مجلد الموقع
- في File Manager، اذهب إلى المجلد `public_html` أو `www` أو `htdocs`
- هذا هو المجلد الرئيسي الذي سيظهر فيه موقعك على الإنترنت
- **ملاحظة:** إذا كان لديك نطاق فرعي أو نطاق رئيسي، تأكد من رفع الملفات في المجلد الصحيح

#### 3. رفع الملفات
- **طريقة أ: رفع ملفات متعددة**
  - اضغط على **Upload** (رفع) في شريط الأدوات
  - اسحب وأفلت جميع ملفات المشروع أو اضغط **Select Files**
  - اختر جميع الملفات التالية:
    - `index.html`
    - `vehicles.html`
    - `riders.html`
    - `settlements.html`
    - `reports.html`
    - `alerts.html`
    - `support.html`
    - `logo.png`
    - `logo.svg`
  - انتظر حتى يكتمل الرفع

- **طريقة ب: رفع مجلدات**
  - اضغط على **Upload** (رفع)
  - ارفع المجلدات `css` و `js` كاملة
  - أو ارفع محتوياتها ملفاً بملف

#### 4. التحقق من الهيكل
بعد الرفع، يجب أن يكون هيكل الملفات كالتالي:
```
public_html/
├── index.html
├── vehicles.html
├── riders.html
├── settlements.html
├── reports.html
├── alerts.html
├── support.html
├── logo.png
├── logo.svg
├── css/
│   └── style.css
└── js/
    └── main.js
```

#### 5. اختبار الموقع
- افتح المتصفح واذهب إلى: `https://yourdomain.com`
- تأكد من أن جميع الصفحات تعمل بشكل صحيح
- تحقق من أن الصور والملفات CSS و JavaScript تعمل

---

## 🔧 الطريقة الثانية: رفع الملفات عبر FTP

### المتطلبات:
- برنامج FTP مثل FileZilla (مجاني) أو WinSCP أو Cyberduck
- بيانات FTP من GoDaddy:
  - FTP Host (مثل: `ftp.yourdomain.com`)
  - FTP Username
  - FTP Password
  - Port (عادة 21)

### الحصول على بيانات FTP:
1. اذهب إلى cPanel
2. ابحث عن **FTP Accounts** (حسابات FTP)
3. ستجد بيانات الاتصال هناك

### الخطوات:

#### 1. تثبيت برنامج FTP
- حمّل [FileZilla](https://filezilla-project.org/) (مجاني ومفتوح المصدر)
- ثبت البرنامج على جهازك

#### 2. الاتصال بالخادم
- افتح FileZilla
- أدخل بيانات FTP:
  - **Host:** `ftp.yourdomain.com` أو عنوان IP المقدم من GoDaddy
  - **Username:** اسم المستخدم FTP
  - **Password:** كلمة المرور FTP
  - **Port:** 21
- اضغط **Quickconnect** (اتصال سريع)

#### 3. رفع الملفات
- في الجانب الأيسر (Local site): اذهب إلى مجلد المشروع على جهازك
- في الجانب الأيمن (Remote site): اذهب إلى `public_html` أو `www`
- اسحب وأفلت جميع الملفات من اليسار إلى اليمين
- أو اختر الملفات واضغط زر **Upload** (رفع)

#### 4. التحقق
- تأكد من رفع جميع الملفات والمجلدات
- افتح الموقع في المتصفح للتحقق

---

## 📁 هيكل الملفات المطلوب

```
/
├── index.html          (الصفحة الرئيسية)
├── vehicles.html       (صفحة المركبات)
├── riders.html         (صفحة المناديب)
├── settlements.html    (صفحة التسويات)
├── reports.html        (صفحة التقارير)
├── alerts.html         (صفحة التنبيهات)
├── support.html        (صفحة الدعم)
├── logo.png            (شعار الشركة)
├── logo.svg            (شعار الشركة SVG)
├── css/
│   └── style.css       (ملف التنسيقات الرئيسي)
└── js/
    └── main.js         (ملف JavaScript الرئيسي)
```

---

## ⚙️ إعدادات مهمة

### 1. ملف .htaccess (اختياري)
يمكنك إنشاء ملف `.htaccess` في مجلد `public_html` لإضافة إعدادات Apache:

```apache
# تفعيل الضغط
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript
</IfModule>

# تفعيل التخزين المؤقت
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
</IfModule>

# إعادة توجيه HTTPS (اختياري)
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
</IfModule>
```

### 2. ملف index.html كصفحة افتراضية
تأكد من أن `index.html` موجود في المجلد الرئيسي (`public_html`)

---

## 🔍 استكشاف الأخطاء وحلها

### المشكلة: الموقع لا يظهر
**الحل:**
- تأكد من رفع `index.html` في المجلد الصحيح (`public_html`)
- تحقق من أن اسم الملف هو `index.html` (بأحرف صغيرة)
- امسح الكاش (Cache) من المتصفح (Ctrl+F5)

### المشكلة: الصور لا تظهر
**الحل:**
- تأكد من رفع `logo.png` في نفس المجلد الذي يحتوي على `index.html`
- تحقق من مسارات الصور في HTML (يجب أن تكون نسبية مثل `logo.png` وليس `/logo.png`)

### المشكلة: التنسيقات (CSS) لا تعمل
**الحل:**
- تأكد من رفع مجلد `css` وملف `style.css` بداخله
- تحقق من مسار CSS في HTML: `href="css/style.css"`
- تأكد من أن المسار صحيح (حساس لحالة الأحرف)

### المشكلة: JavaScript لا يعمل
**الحل:**
- تأكد من رفع مجلد `js` وملف `main.js` بداخله
- تحقق من مسار JavaScript في HTML: `src="js/main.js"`
- افتح Console في المتصفح (F12) للتحقق من الأخطاء

### المشكلة: الصفحات تعرض 404
**الحل:**
- تأكد من رفع جميع ملفات HTML
- تحقق من أسماء الملفات (يجب أن تكون مطابقة تماماً)
- تأكد من أن الروابط في HTML صحيحة

### المشكلة: الموقع بطيء
**الحل:**
- فعّل الضغط (Compression) في `.htaccess`
- فعّل التخزين المؤقت (Caching)
- استخدم CDN للخطوط والصور الخارجية (موجود بالفعل في الكود)

---

## 🔐 الأمان

### 1. تفعيل HTTPS
- تأكد من تفعيل شهادة SSL في GoDaddy
- اذهب إلى cPanel > SSL/TLS Status
- فعّل SSL للنطاق

### 2. حماية الملفات الحساسة
- لا ترفع ملفات `.env` أو ملفات الإعدادات الحساسة
- احمِ ملف `.htaccess` من الوصول المباشر

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من [دليل GoDaddy](https://www.godaddy.com/help)
2. تواصل مع دعم GoDaddy
3. راجع ملفات السجل (Logs) في cPanel

---

## ✅ قائمة التحقق النهائية

قبل الإعلان عن الموقع، تأكد من:

- [ ] جميع الملفات مرفوعة بشكل صحيح
- [ ] الصفحة الرئيسية (`index.html`) تعمل
- [ ] جميع الروابط بين الصفحات تعمل
- [ ] الصور تظهر بشكل صحيح
- [ ] التنسيقات (CSS) تعمل
- [ ] JavaScript يعمل
- [ ] الموقع يعمل على HTTPS
- [ ] الموقع يعمل على الأجهزة المحمولة (Responsive)
- [ ] جميع النماذج (Forms) تعمل (إن وجدت)

---

## 🎉 تهانينا!

بعد اكتمال جميع الخطوات، سيكون موقعك متاحاً على الإنترنت!

**رابط الموقع:** `https://yourdomain.com`

---

## 📝 ملاحظات إضافية

- **النسخ الاحتياطي:** احرص على عمل نسخة احتياطية من الملفات قبل أي تعديلات
- **التحديثات:** عند تحديث الموقع، ارفع الملفات المعدلة فقط
- **الأداء:** استخدم أدوات مثل [Google PageSpeed Insights](https://pagespeed.web.dev/) لتحسين الأداء

---

**آخر تحديث:** 2025

